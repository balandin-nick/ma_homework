apiVersion: apps/v1
kind: Deployment
metadata:
  name: ma_homework
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ma_homework
  template:
    metadata:
      labels:
        app: ma_homework
    spec:
      containers:
      - name: ma_homework
        image: nickfeld/ma_homework:0.1.0
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - name: web
          containerPort: 8000
