apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ma-homework
spec:
  rules:
  - host: arch.homework
    http:
      paths:
        - path: /otusapp
          pathType: Prefix
          backend:
            service:
              name: ma-homework
              port:
                number: 9000